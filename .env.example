

# Optional: Redis URL for cache migration. Leave unset to use the server's in-memory
# Map-based cache (recommended for local/laptop use to avoid extra hosting cost).
REDIS_URL=
EBAY_CLIENT_ID=your-ebay-client-id
EBAY_CLIENT_SECRET=your-ebay-client-secret
PORT=5001

# Frontend (Vite) flags
VITE_DEV_TOOLS=1
VITE_SUGGEST_SERVER=1
VITE_SUGGEST_EBAY=1

# Backend (Express) helpers
SEED_SUGGESTIONS=1
ALLOW_CACHE_CLEAR=1
ENABLE_TRACE_LOG=0
EBAY_MARKETPLACE_ID=EBAY_US

GEMINI_API_KEY=your-gemini-api-key
GEMINI_MAX_WIDTH=1024
GEMINI_MODELS=gemini-2.5-flash,gemini-2.0-flash-exp,gemini-2.0-flash
AI_SCAN_MAX_PER_MIN=5


OCR_SPACE_API_KEY=your-ocr-space-api-key